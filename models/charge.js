
module.exports = {
    charge : class Charge {

        constructor(userName, amount, chargeDate, objective){
                this.userName = userName;
                this.amount = amount;
                this.chargeDate = chargeDate;
                this.objective = objective;
        }
    },
} 

// module.exports = User;